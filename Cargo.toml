[package]
name = "aros"
version = "0.1.0"
authors = ["k0pernicus <antonin.carette@gmail.com>"]

[lib]
crate-type = ["staticlib"]

[dependencies]
rlibc = "0.1"
x86_64 = "0.3.1"
vga = { path = "src/vga" }
interrupts = { path = "src/interrupts" }

[profile.release]
panic = "abort"
